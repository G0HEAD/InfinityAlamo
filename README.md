# InfinityAlamo

Automated daily Texas probate extraction and PDF parsing pipeline.

## Quick start

- Create a virtual environment and install dependencies.
- Copy `.env.example` to `.env` and fill in required values.
- Configure `config/counties.yaml`.
- Run with `python -m probate.cli --yesterday`.

## Status
Milestone 1 foundations in progress.
